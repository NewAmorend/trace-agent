# Agent Trajectory Eval — dev dependencies
#
# Runtime: stdlib only (no dependencies).
# Tests:   unittest (stdlib).
#
# Fetchers (lcb.py, swe.py) talk to Hugging Face over direct HTTP,
# so even the optional fetcher path is stdlib-only.
#
# Install with:
#   pip install -r requirements-dev.txt

ruff>=0.4
mypy>=1.10
coverage>=7.0
